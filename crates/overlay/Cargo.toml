[package]
name = "tessera-overlay"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
tessera-render = { path = "../render" }
tessera-term = { path = "../term" }
wgpu = "=29.0.3"
winit = "=0.30.9"
tracing = { workspace = true }
uuid = { workspace = true }
raw-window-handle = "0.6"

[target.'cfg(all(unix, not(target_os = "macos")))'.dependencies]
x11-dl = "2.21"

[dev-dependencies]
wgpu = "=29.0.3"
